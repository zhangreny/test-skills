interface:
  display_name: "Figma SVG+PNG UI Case Generate"
  short_description: "Generate UI testcases from doc plus per-scene Figma PNG and SVG assets."
  default_prompt: "Use $figma-2-svg-and-png-doc-uicase-generate to split a Figma board into the largest scene blocks, prepare PNG and SVG for each scene, then draft layered UI testcases from the requirement doc and those scene assets."
