interface:
  display_name: "SmartX Testcase Generate"
  short_description: "按 lite / standard / deep 模式生成 SmartX TestRail 测试用例，并在终稿前执行 finalcheck 记忆校验。"
  brand_color: "#2563EB"
  default_prompt: "Use $testcase-generate to analyze my SmartX requirement documents, choose the right workflow mode, generate TestRail-style testcases, and run the finalcheck memory review before returning the final draft."

policy:
  allow_implicit_invocation: true
