interface:
  display_name: "SmartX Testcase Generate"
  short_description: "先确认本轮输入资料，再按固定完整流程生成 SmartX TestRail 测试用例，并在终稿前执行 finalcheck 记忆校验。"
  brand_color: "#2563EB"
  default_prompt: "Use $testcase-generate to intake my SmartX requirement files, folders, uploads, or Google Docs links, confirm the input scope, run the full fixed workflow from Step 0 through Step 17, generate TestRail-style testcases, and run the finalcheck memory review before returning the final draft."

policy:
  allow_implicit_invocation: true
