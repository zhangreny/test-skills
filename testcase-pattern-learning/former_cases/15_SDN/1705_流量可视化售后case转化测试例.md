demo demo
    demo1
        section1
            [Demo] Case 1 - 基础功能测试
            [Demo] Case 2 - 验证用例字段
            section2
                [Demo] Case 3 - 层级 Section 测试
generate
    vds 支持组播
        编辑限制
            [VDS] 编辑 IGMP/MLD 侦听时不能同时修改网口绑定模式
            [VDS] 编辑 IGMP/MLD 侦听时不能同时进行单网口变为多网口操作
            [VDS] 编辑 IGMP/MLD 侦听时不能同时为新主机关联网口
        IGMP
            MLD Snooping 功能
                [VDS] 启用 IGMP/MLD 侦听后支持 IGMPv1 协议
                [VDS] 启用 IGMP/MLD 侦听后支持 IGMPv2 协议
                [VDS] 启用 IGMP/MLD 侦听后支持 IGMPv3 协议
                [VDS] 启用 IGMP/MLD 侦听后支持 MLDv1 协议
                [VDS] 启用 IGMP/MLD 侦听后支持 MLDv2 协议
                [VDS] 启用后能侦听虚拟机的 IGMP/MLD 报文
                [VDS] 启用后能动态维护组播组成员
                [VDS] 启用后能定时发送查询报文
                [VDS] 启用后组播流量精准转发不泛洪
                [VDS] 禁用后组播流量恢复泛洪机制
                [VDS] 多个虚拟机加入同一组播组时精准转发
                [VDS] 多个虚拟机加入不同组播组时精准转发
        集成与升级
            [VDS] 升级后原有 VDS 的 IGMP/MLD 侦听默认为禁用状态
            [VDS] 升级后可以手动启用 IGMP/MLD 侦听
            [VDS] Everoute 默认放通 IGMP 协议流量到组播 IP 段
            [VDS] Everoute 默认放通 MLD 协议流量到组播 IP 段
            [VDS] 组播 IP 作为 Everoute 安全策略对象时策略生效
            [VDS] 组播组成员虚拟机作为 Everoute 安全策略对象时的行为
        VDS 基础功能
            [VDS] 创建时默认 IGMP/MLD 侦听为禁用状态
            [VDS] 创建时可手动启用 IGMP/MLD 侦听
            [VDS] 编辑时可启用 IGMP/MLD 侦听
            [VDS] 编辑时可禁用 IGMP/MLD 侦听
            [VDS] 列表页面展示 IGMP/MLD 侦听状态
            [VDS] 列表支持按 IGMP/MLD 侦听状态筛选
            [VDS] 列表支持按 IGMP/MLD 侦听状态排序
            [VDS] 详情页面展示 IGMP/MLD 侦听状态
            [VDS] 网络拓扑不展示 IGMP/MLD 侦听字段
    vds 支持组播-1
        组合场景
            [VDS] 存储 VDS Single 模式下启用组播 snooping 功能验证
            [VDS] 存储 VDS Chain 模式下启用组播 snooping 功能验证
            [VDS] 非存储 VDS Single 模式下启用组播 snooping 功能验证
            [VDS] 非存储 VDS Chain 模式下启用组播 snooping 功能验证
            [VDS] 单网口配置下启用组播 snooping 功能验证
            [VDS] Bond 配置（单活模式）下启用组播 snooping 功能验证
            [VDS] Bond 配置（主备模式）下启用组播 snooping 功能验证 - 主网口 active
            [VDS] Bond 配置（主备模式）下启用组播 snooping 功能验证 - 备网口 active
            [VDS] 网口配置变更：从单网口变为 Bond 时组播 snooping 配置一致性
            [VDS] 网口配置变更：从 Bond 变为单网口时组播 snooping 配置一致性
            [VDS] 同 VLAN、同节点场景下组播转发
            [VDS] 同 VLAN、不同节点场景下组播转发
            [VDS] 不同 VLAN 场景下组播隔离
            [VDS] IPv4 不分片组播流量转发
            [VDS] IPv4 分片组播流量转发
            [VDS] IPv6 不分片组播流量转发
            [VDS] IPv6 分片组播流量转发
            [VDS] RDMA 开启时组播 snooping 功能验证
            [VDS] RDMA 关闭时组播 snooping 功能验证
        边界情况
            [VDS] 节点重启前检查组播 snooping 配置和 MDB 表
            [VDS] 节点重启后检查组播 snooping 配置恢复
            [VDS] 节点重启后组播转发功能验证
            [VDS] 节点掉电前检查组播 snooping 配置和 MDB 表
            [VDS] 节点掉电恢复后检查组播 snooping 配置恢复
            [VDS] 节点掉电恢复后组播转发功能验证
            [VDS] mcast-agent 服务停止后组播 snooping 功能验证
            [VDS] mcast-agent 服务恢复后查询报文发送验证
            [VDS] 升级前启用组播 snooping 的 VDS，升级后配置保持
            [VDS] 升级前禁用组播 snooping 的 VDS，升级后可启用
            [VDS] 扩容节点后新节点上 VDS 的组播 snooping 配置正确
            [VDS] 扩容节点后跨节点组播转发功能验证
            [VDS] 缩容节点前组播组成员迁移验证
            [VDS] 缩容节点后剩余节点组播转发功能验证
            [Network-tool] change-bridge-mcast 命令参数错误：ovsbr_name 不存在
            [Network-tool] change-bridge-mcast 命令参数错误：mcast_snooping 参数格式错误
            [Network-tool] change-bridge-mcast 命令参数错误：uplink_port 参数错误
            [Network-tool] change-bridge 命令缺少 mcast_snooping 参数时配置一致性验证
            [Network-tool] change-bridge 命令指定 mcast_snooping 参数时配置正确
        编辑限制
            [VDS] 编辑 IGMP/MLD 侦听时不能同时修改网口绑定模式
            [VDS] 编辑 IGMP/MLD 侦听时不能同时进行单网口变为多网口操作
            [VDS] 编辑 IGMP/MLD 侦听时不能同时为新主机关联网口
            [VDS] 存储 VDS Single 模式下编辑 IGMP/MLD 侦听时不能同时修改网口绑定模式
            [VDS] 非存储 VDS Chain 模式下编辑 IGMP/MLD 侦听时不能同时进行单网口变为多网口操作
            [VDS] 存储 VDS Chain 模式下编辑 IGMP/MLD 侦听时不能同时修改网口绑定模式
            [VDS] 非存储 VDS Single 模式下编辑 IGMP/MLD 侦听时不能同时为新主机关联网口
            [VDS] 非存储 VDS Chain 模式下编辑 IGMP/MLD 侦听时不能同时为新主机关联网口
        IGMP
            MLD Snooping 功能
                [VDS] 启用 IGMP/MLD 侦听后支持 IGMPv1 协议
                [VDS] 启用 IGMP/MLD 侦听后支持 IGMPv2 协议
                [VDS] 启用 IGMP/MLD 侦听后支持 IGMPv3 协议
                [VDS] 启用 IGMP/MLD 侦听后支持 MLDv1 协议
                [VDS] 启用 IGMP/MLD 侦听后支持 MLDv2 协议
                [VDS] 启用后能侦听虚拟机的 IGMP/MLD 报文
                [VDS] 启用后能动态维护组播组成员
                [VDS] 启用后能定时发送查询报文
                [VDS] 启用后组播流量精准转发不泛洪
                [VDS] 禁用后组播流量恢复泛洪机制
                [VDS] 多个虚拟机加入同一组播组时精准转发
                [VDS] 多个虚拟机加入不同组播组时精准转发
                [VDS] 启用后立即检查组播 snooping 配置
                [VDS] 编辑前后检查组播 snooping 配置变化
                [VDS] 同 VLAN、同节点场景下组播转发
                [VDS] 同 VLAN、不同节点场景下组播转发
                [VDS] 不同 VLAN 场景下组播隔离
                [VDS] IPv4 分片组播流量转发
                [VDS] IPv6 分片组播流量转发
        集成与升级
            [VDS] 升级后原有 VDS 的 IGMP/MLD 侦听默认为禁用状态
            [VDS] 升级后可以手动启用 IGMP/MLD 侦听
            [VDS] Everoute 默认放通 IGMP 协议流量到组播 IP 段
            [VDS] Everoute 默认放通 MLD 协议流量到组播 IP 段
            [VDS] 组播 IP 作为 Everoute 安全策略对象时策略生效
            [VDS] 组播组成员虚拟机作为 Everoute 安全策略对象时的行为
            [VDS] 存储 VDS Single 模式下升级后原有 VDS 的 IGMP/MLD 侦听默认为禁用状态
            [VDS] 存储 VDS Chain 模式下升级后可以手动启用 IGMP/MLD 侦听
            [VDS] 非存储 VDS Single 模式下升级后原有 VDS 的 IGMP/MLD 侦听默认为禁用状态
            [VDS] 非存储 VDS Chain 模式下升级后可以手动启用 IGMP/MLD 侦听
        VDS 基础功能
            [VDS] 创建时默认 IGMP/MLD 侦听为禁用状态
            [VDS] 创建时可手动启用 IGMP/MLD 侦听
            [VDS] 编辑时可启用 IGMP/MLD 侦听
            [VDS] 编辑时可禁用 IGMP/MLD 侦听
            [VDS] 列表页面展示 IGMP/MLD 侦听状态
            [VDS] 列表支持按 IGMP/MLD 侦听状态筛选
            [VDS] 列表支持按 IGMP/MLD 侦听状态排序
            [VDS] 详情页面展示 IGMP/MLD 侦听状态
            [VDS] 网络拓扑不展示 IGMP/MLD 侦听字段
            [VDS] 存储 VDS Single 模式下创建时启用组播 snooping
            [VDS] 非存储 VDS Chain 模式下创建时启用组播 snooping