文件存储集群
    事件/任务相关校验
        生命周期相关（部署、升级、扩容、上线、下线、编辑、删除）任务的开始/结束时间、任务运行顺序均应符合预期
final build POC 测试项- P0
    后端物理集群常见故障测试
        后端物理集群掉电测试
        后端物理节点掉电测试
        后端物理节点存储网卡故障测试
    物理集群平台及其他关联产品的适配测试
        对OS最新新版本的适配测试
        对OS历史版本的适配测试（需具体分析）
        对ZBS最新版本的适配测试
        对ZBS历史版本的适配测试
        对ACOS最新版本的适配测试
        对ACOS 历史版本的适配测试
        对OBS/Tower 最新版本的适配测试
        对OBS/Tower历史版本的适配测试
Tower460 相关测试
    静态路由子网掩码优化验证
    网络配置子网掩码优化验证
    operator 配额优化
        resource 配置检查
        cpu 配额有效性检验：基线监控
        cpu 配额有效性检验：压力测试
        cpu 配额有效性检验：代码注入 operator 使 cpu 超限，验证 cpu 超限不会导致 pod 重启
        内存配额动态调整有效性验证：默认阈值测试
        内存配额动态调整有效性验证：代码注入强制内存超限后 OOM 重建 pod
        内存配额动态调整有效性验证：超阈值触发调整 limit，验证调整公式准确性
        升级测试
        兼容性测试
    SFS-578、SFS-777、SFS-761（tower 4.6.0 + sfs 1.2.0）
        需回归所有iomesh中所有会触发task的用例（tower 4.6.0 + 1.2.0）
        需回归所有arcfra中所有会触发event的用例（tower 4.6.0 + 1.2.0）
        需回归所有iomesh中所有会触发event的用例（tower 4.6.0 + 1.2.0）
        需回归所有arcfra中所有会触发task的用例（tower 4.6.0 + 1.2.0）
        在 task 执行过程中重启 sfs-operator，预期 UI 中的 task 能够被正常结束
    SFS-620 (tower 4.6.0 + sfs 1.3.0)
        升级文件存储集群 ：小于1.3.0--->1.3.0，预期成功，kube_vip不存在
        部署 1.3.0 版本的4节点集群，部署成功，kube_vip不存在
        对管理VIP所在的FSC进行下线操作，前/后端均需检查管理VIP迁移至其它未下线节点，并且只有本节点为更新中，其它节点均为正常
        管理 VIP所在的文件控制器的文件管理网卡故障后，前/后端均需检查文件管理VIP发生迁移
        管理 VIP所在的文件控制器的apiserver挂掉，前/后端均需检查文件管理VIP发生迁移
        管理VIP所在的文件控制器的文件存储网卡故障后，前/后端均需检查文件管理VIP发生迁移
        将管理VIP通过修改ManagementVip.status.assigned_node leader 至其它节点，前/后端均需检查文件管理VIP发生迁移
        挂掉2个ETCD后，将管理 VIP所在的文件控制器的文件管理网卡对应的物理网卡故障后，此时管理VIP无法进行迁移，恢复其中1个ETCD，前/后端均需检查文件管理VIP发生迁移
        先升级sfs集群到1.3.0，然后升级tower从4.5.0到4.6.0，预期成功，kube_vip不存在，能够访问managementvip
    SFS-451
        不在更新sfs集群内部的cp elf token
    SFS-798
        当下线 vip&meta&manager leader所在的文件控制器，此时I/O会发生90s超时
    自动设置DRS调度黑名单
        Tower升级场景
            打开了DRS调度的Tower, 且部署了SFS，当Tower升级到460之后，该 SFS 集群所有的 FSC 会被自动添加到 特例规则/关闭动态资源调度表中，见下图。
            ACOS平台-打开了DRS调度的Tower, 且部署了SFS，当Tower升级到460之后，该 SFS 集群所有的 FSC 会被自动添加到 特例规则/关闭动态资源调度表中，见下图。
        DRS 操作场景-Tower
            打开DRS: tower 460 版本， 已经部署了SFS集群，但未打开DRS, 手动打开DRS后，SFS 集群所有的 FSC 会被自动添加到 特例规则/关闭动态资源调度表中。
            【关闭 DRS，再打开】tower 460 版本， 已经部署了SFS集群，且SFS 集群所有的 FSC 已经被自动添加到 特例规则/关闭动态资源调度表中后，关闭 DRS，再打开后，确保SFS 集群所有的 FSC 已经被自动添加到 特例规则/关闭动态资源调度表中。
            【手动编辑DRS/动态关闭资源调度场景】tower 460 版本， 已经部署了SFS集群，且SFS 集群所有的 FSC 已经被自动添加到 特例规则/关闭动态资源调度表中后，手动移除 FSC，等待一段时间，确被移除的 FSC 会被自动添加到 特例规则/关闭动态资源调度表中。
            【手动编辑DRS/动态关闭资源调度场景】tower 460 版本， 已经部署了SFS集群，且SFS 集群所有的 FSC 已经被自动添加到 特例规则/关闭动态资源调度表中后，手动移除 FSC，且将该FSC加到到任意一个其他特例规则中，等待一段时间，确被移除的 FSC 会被自动添加到 特例规则/关闭动态资源调度表中。
        DRS 操作场景-AOC
            【打开DRS】AOC460 版本， 已经部署了SFS集群，但未打开DRS, 手动打开DRS后，SFS 集群所有的 FSC 会被自动添加到 特例规则/关闭动态资源调度表中。
            【关闭 DRS，再打开】AOC460 版本， 已经部署了SFS集群，且SFS 集群所有的 FSC 已经被自动添加到 特例规则/关闭动态资源调度表中后，关闭 DRS，再打开后，确保SFS 集群所有的 FSC 已经被自动添加到 特例规则/关闭动态资源调度表中。
            【手动编辑DRS/动态关闭资源调度场景】AOC460 版本， 已经部署了SFS集群，且SFS 集群所有的 FSC 已经被自动添加到 特例规则/关闭动态资源调度表中后，手动移除 FSC，等待一段时间，确被移除的 FSC 会被自动添加到 特例规则/关闭动态资源调度表中。
            【手动编辑DRS/动态关闭资源调度场景】AOC460 版本， 已经部署了SFS集群，且SFS 集群所有的 FSC 已经被自动添加到 特例规则/关闭动态资源调度表中后，手动移除 FSC，且将该FSC加到到任意一个其他特例规则中，等待一段时间，确被移除的 FSC 会被自动添加到 特例规则/关闭动态资源调度表中。
        SFS 操作相关-Tower
            【扩容SFS】tower 460 版本， 已经部署了SFS集群，对SFS进行扩容，扩容后，确保SFS 集群新增FSC节点已经被自动添加到 特例规则/关闭动态资源调度表中。
            【新部署SFS】tower 460 版本， 新部署了SFS集群，部署完成后，确保SFS 集群所有FSC节点已经被自动添加到 特例规则/关闭动态资源调度表中。
            【删除SFS】tower 460 版本， 卸载SFS集群，卸载完成后，确保已经被卸载的SFS集群所有FSC节点已经被自动从 特例规则/关闭动态资源调度表中移除。
        AFS 操作相关-AOC
            【扩容AFS】AOC 460 版本， 已经部署了SFS集群，对SFS进行扩容，扩容后，确保SFS 集群新增FSC节点已经被自动添加到 特例规则/关闭动态资源调度表中。
            【新部署AFS】AOC460 版本， 新部署了SFS集群，部署完成后，确保SFS 集群所有FSC节点已经被自动添加到 特例规则/关闭动态资源调度表中。
            【删除AFS】 AOC 460 版本， 卸载SFS集群，卸载完成后，确保已经被卸载的SFS集群所有FSC节点已经被自动从 特例规则/关闭动态资源调度表中移除。
SFS 1.2.1
    安装包管理
        上传安装包
            上传 Arm架构的安装包，能够上传成功
            拖拽 Arm架构的安装包，能够上传成功
            上传版本相同但架构不同的安装包，能够上传成功
            上传重复的安装包（架构、版本均相同），上传100%后，解析失败
        删除安装包
            当不存在使用该安装包部署的文件存储集群，删除安装包
            当前存在使用该安装包部署的文件存储集群，删除该安装包
            使用sfs 1.2.1-rc.A 部署文件存储集群，升级到sfs 1.2.1-rc.B，安装包sfs 1.2.1-rc.A删除成功，事件和任务中展示成功
            当前存在使用该安装包部署的文件存储集群，删除文件存储集群,提示应只展示同架构的文件存储集群名称
    部署文件存储集群
        版本
            当存在版本相同但架构分别为X86_64和AArch64架构的sfs版本（>1.2.0）
            仅存在sfs 1.2.1版本（X86_64架构）
            仅存在sfs 1.2.1版本（AArch64架构）
        部署位置
            OS（6.1.0及以上）
                当前已关联ARM架构的OS集群，上传 X86_64架构的sfs对应版本的安装包
                当前已关联X86_64架构的OS集群，上传ARM 架构的sfs对应版本的安装包
                当前已关联ARM架构的OS集群，上传 ARM架构的sfs安装包
                若未在ARM架构的OS集群--->设置--->虚拟 IP中设置管理虚拟IP
                若ARM架构的OS集群的主机数小于3个
            ZBS（暂定5.6.2）
                当前已关联ARM架构的ZBS集群，上传 X86_64架构的sfs对应版本的安装包
                当前已关联X86_64架构ZBS集群，上传ARM 架构的sfs对应版本的安装包
                当前已关联ARM架构的ZBS集群，上传 ARM架构的sfs安装包
                若未在ARM架构的ZBS集群--->设置--->虚拟 IP中设置管理虚拟IP
                若ARM架构的ZBS集群的主机数小于3个
        vCPU
            OS
                ARM
                    部署文件存储集群，vCPU默认填充8
                    vCPU输入数值7，提示「请输入8-240 之间的整数」
                    vCPU输入数值240，进入到下一步
                    vCPU输入数值241，提示「请输入8-240之间的整数」
                    文件存储集群部署成功后，查看相关设置符合预期
                    删除文件存储集群，该主机所在的已独占vCPU数量 减 8
                x86_64
                    部署文件存储集群，vCPU默认填充4
                    部署文件存储集群，vCPU输入数值3，提示「请输入4-240之间的整数」
            ZBS
                ARM
                    部署文件存储集群，vCPU默认填充16
                    vCPU输入数值7，提示「请输入8-240 之间的整数」
                    vCPU输入数值240，进入到下一步
                    vCPU输入数值241，提示「请输入8-240之间的整数」
                x86_64
                    部署文件存储集群，vCPU默认填充16
                    部署文件存储集群，vCPU输入数值3，提示「请输入4-240之间的整数」
    文件存储集群概览
        SFS 部署在 Arm 架构的集群上，在文件存储集群概览的「集群信息」 widget 中，「CPU 架构」展示为「AArch64」
    文件存储集群设置
        文件存储集群设置--->集群升级---->「CPU 架构」中，如果是 Arm 架构，「CPU 架构」展示为「AArch64」
    升级文件存储集群
        部署X86_64架构的sfs 1.2.1-A 版本，上传ARM架构的sfs 1.2.1-B版本（大于sfs 1.2.1-A版本）
        部署ARM架构的sfs 1.2.1-A 版本，上传X86_64架构的sfs 1.2.1-B版本（大于sfs 1.2.1-A版本）
Tower 4.7.0 相关
    operator 支持为 SMTX ZBS 的 hygon CPU 开启独占
        部署文件存储集群
            输入CPU 独占数量，若满足要求的主机大于或等于文件控制器数量，则不提示相关信息。集群部署成功后，通过查看vm的基本信息，在vCPU处显示「独占已生效」
            输入CPU 独占数量，若满足要求的主机小于文件控制器数量，则提示「”集群内满足文件控制器 CPU 独占要求的主机为 n 个，少于期望的文件控制器数量。"」
            后端修改sfscluster cpuExclusive 从 true改为false，会关闭独占
        升级文件存储集群
            部署ZBS 5.7.0及以上 + hygon + Tower 4.7.0 + sfs 1.2.1，升级至1.3.0
            部署小于 ZBS 5.7.0  + hygon + Tower 4.7.0 + sfs 1.3.0，升级至ZBS5.7.0
            ZBS 5.7.0及以上 + 非 hygon + Tower 4.7.0 + sfs 1.3.0
        删除文件存储集群
            文件存储集群 删除文件存储集群并且删除成功后，释放独占的 vCPU
        编辑文件存储集群
            编辑计算资源
                增加vCPU数量，已有的文件控制器所在的主机均满足新填写 vCPU 数的 CPU 独占要求，则不提示相关信息。集群编辑成功后，通过查看vm的基本信息，在vCPU处显示「独占已生效」，并且vCPU数量与编辑数量一致
                增加vCPU数量，存在 1个 已有的文件控制器所在的主机小于新填写 vCPU 数的 CPU 独占要求，提示“存在已有的文件控制器 %fsc-name%，其所在主机不满足调整后的 CPU 独占要求。”。
                增加vCPU数量，存在 2个及以上 已有的文件控制器所在的主机小于新填写 vCPU 数的 CPU 独占要求，提示“%n% 个文件控制器所在的主机不满足调整后的 CPU 独占要求。”。
            增加文件控制器个数
                增加文件控制器数量，集群内未部署当前文件存储集群文件控制器 && 满足可独占 vCPU 数量 的主机数 需大于等于 计划扩容的文件控制器数，扩容文件存储集群成功，该看扩容vm的基本信息，在vCPU处显示「独占已生效」
                增加文件控制器数量，集群内未部署当前文件存储集群文件控制器 && 满足可独占 vCPU 数量 的主机数 小于 计划扩容的文件控制器数，提示“集群内未部署当前文件存储集群文件控制器的主机中满足 CPU 独占要求的主机为 n 个，少于期望新增的文件控制器数量。
    增加文件系统使用率过高的报警项
        触发报警和消除报警
        禁用报警规则和启用报警规则
        修改报警规则
        添加特例规则
        扩容已触发报警的文件系统
        将已触发报警的文件系统下线
        删除已触发报警的文件系统
        升级 AOC/CloudTower 和 AFS/SFS
    仅允许在高配 CloudTower 中部署文件存储集群
        低配 AOC/CloudTower 提升为高配 AOC/CloudTower
        高配 AOC/CloudTower 部署文件存储集群
        低版本低配 AOC/CloudTower 已点击过“开始试用”，但从未上传过文件存储服务安装包，升级后提升为高配 AOC/CloudTower
        低版本低配 AOC/CloudTower 已经上传过文件存储服务安装包，升级前已删除所有文件存储服务安装包
        低版本低配 AOC/CloudTower 已经上传了文件存储服务安装包，在文件存储服务安装包存在的情况下升级 AOC/CloudTower，升级后提升为高配 AOC/CloudTower
        低版本低配 AOC/CloudTower 已经部署过文件存储集群，升级前已删除所有文件存储集群和所有文件存储服务安装包
        低版本低配 AOC/CloudTower 已经部署过文件存储集群，升级前已删除所有文件存储集群，在文件存储服务安装包存在的情况下升级 AOC/CloudTower
        低版本低配 AOC/CloudTower 已经部署了文件存储集群，在文件存储集群存在的情况下升级 AOC/CloudTower，升级后提升为高配 AOC/CloudTower
    文件存储集群组件状态异常报警文案优化
        API 组件 k8s-api-server 和 etcd 异常
        文件服务组件 sfs-manager 异常
        文件服务组件 sfs-cloud-provider 异常
    界面上增加文件存储集群是否支持 Windows NFS 的勾选项
        部署SFS集群
            UI相关（iomesh 和 arcfra 都覆盖）
                【Tower 470+SFS1.2.x】部署时，新增「访问配置」，相关文案如下。
                【Tower 470+SFS1.3.0】部署时，新增「访问配置」，相关文案如上。
                【Tower 470+SFS1.2.0以下】部署SFS 1.1.2 时，无「访问配置」选项。
                【Tower470以下版本 + SFS 1.3.0】不支持该场景适配，不做测试。
            全链路相关
                【Tower 470+SFS1.2.x】部署时，新增「访问配置」，打开「windows支持NFS 访问」，可以部署成功。
                【Tower 470+SFS1.3.0】部署时，新增「访问配置」，打开「windows支持NFS 访问」，可以部署成功。
                【Tower 470+SFS1.2.x】部署时，新增「访问配置」，不打开「windows支持NFS 访问」，可以部署成功。
                x【Aoc 470+AFS1.3.0】部署时，新增「访问配置」，打开「windows支持NFS 访问」，可以部署成功。
                【AOC 470+AFS1.3.0】部署时，新增「访问配置」，不打开「windows支持NFS 访问」，可以部署成功。
        升级SFS集群
            【Tower 461 + SFS 1.2.x  + 手动打开 「windows nfs 访问」】【iomesh 平台】
            【Tower 461 + SFS 1.2.x  + 未打开 「windows nfs 访问」】【iomesh 平台】
            【Tower 461 + SFS 1.1.x 】（iomesh 和 arcfra 都覆盖）
            【Tower 461 + SFS 1.2.x  + 关闭 「windows nfs 访问」】（arcfra 测试）
            【Tower 470 + SFS 1.1.x  部署之后，升级到 SFS 130】
        编辑SFS集群
            【Tower470+ SFS1.2.x- 未启用 「windows 支持nfs」--> 启用 「windows 支持nfs」 】
            【Tower470+ SFS1.2.x- 启用 「windows 支持nfs」--> 关闭「windows 支持nfs」 】
            【Tower470+ SFS1.3.0，未打开 →打开 「windows 支持nfs」】（iomesh 和 arcfra 都覆盖）
            【Tower470+ SFS1.3.0- 启用 「windows 支持nfs」--> 关闭「windows 支持nfs」 】 （iomesh 和 arcfra 都覆盖）
        其他场景
            【FSC 下线】单个FSC 下线之后， 集群的 「windows nfs 访问」可以正常展示为实际的值（iomesh 和 arcfra 都覆盖）
            【SFS 下线】SFS集群 下线之后， 集群的 「windows nfs 访问」展示为 “-”
            UI 新增字段编辑搜索场景（iomesh 和 arcfra 都覆盖）
    多 chunk 实例 SFS EC 「可提供服务的主机数」调整
        ZBS 570  + Tower 4.7.0 + sfs 1.3.0
            4节点ZBS 集群，任一主机至少有一个chunk（最好为每个主机仅有一个chunked）处于健康且非移除中的状态，此时允许点击「纠删码」，Hover配比文案调整 : 「K + M + 1 小于等于集群中可提供存储服务（包含健康且非移除中的存储服务）的主机数。」
            4节点ZBS集群，对任意一个主机输入以下命令，禁用所有chunk； systemctl stop zbs-chunkd   此时EC被禁用，hover info icon 展示的 Tooltip 内文案其中第二条更新为：至少 4 个可提供存储服务（包含健康且非移除中的存储服务）的主机
        升级场景
            ZBS 5.6.3 + 【Tower 4.7.0】 + sfs 1.2.1
            ZBS 5.6.3 + 【Tower 4.7.0】 + 【sfs 1.3.0】
            【ZBS 5.7.0】 + 【Tower 4.7.0】 + sfs 1.2.1
            【ZBS 5.7.0】+ 【Tower 4.7.0】 + 【sfs 1.3.0】
    sfs-operator 无需维护常驻缓存是否能开和是否开启的判断逻辑
        部署文件存储集群
            集群采用单一存储介质的不分层模式部署
                tower470 上部署 sfs1.3.0 +  成功且无告警，系统盘不会自动开启常驻缓存
            集群采用分层模式部署 && 不是单一存储介质
                物理集群未开启常驻缓存
                    通过前端查看 prio_space_percentage = false
                    前端尝试部署 sfs130 在未开启常驻缓存的 ZBS 集群上，选择部署位置时列表提示【集群未启用数据缓存模式】
                    尝试部署 sfs130 在未开启常驻缓存的 ZBS 集群上，通过后端部署 sfscluster 时 webhook 报错 SFS_OPERATOR_BASE_CLUSTER_VOLUME_PINNING_NOT_ENABLED
                物理集群开启常驻缓存
                    通过前端查看prio_space_percentage = true
                    zbs 空闲常驻缓存空间足够时，前/后部署成功且自动开启常驻缓存
                    zbs空闲常驻缓存空间不够时，前端需进行校验
        升级场景
            物理集群开启常驻缓存
                部署文件存储集群成功，并且常驻缓存均已开启
                当组合为：小于 ZBS 5.7.0 + Tower 4.7.0 + sfs 1.2.1，常驻缓存均为已开启
                当组合为：ZBS 5.7.0 + Tower 4.7.0 + sfs 1.2.1，常驻缓存均为已开启
                当组合为：ZBS 5.7.0 + Tower 4.7.0 + sfs 1.3.0，常驻缓存均为已开启
            物理集群未开启常驻缓存
                【小于 ZBS 5.7.0 + Tower 4.7.0 + sfs 1.1.1 】升级 至 【ZBS 5.7.0 + Tower 4.7.0 + sfs 1.3.0】，UI弹窗报错并阻止升级、升级前存在告警
        变更场景
            Tower 4.7.0 + sfs 1.1.0 部署在 os 600，采用分层模式部署 && 不是单一存储介质部署，但集群未开启常驻缓存，不需要校验强制开启常驻缓存，无告警，部署成功
    限制源版本为 1.2.0 时只能升级至 1.2.1-pre1
        Tower 4.6.2
            【前/后段均校验】部署SFS 1.1.2 文件存储集群，升级到SFS 1.2.1-pre1 报错,错误码："SFS_OPERATOR_CLUSTER_VERSION_REQUIRED": "选择的版本不合法。
            升级至 SFS 1.2.0成功， 进行除升级外的编辑操作，编辑成功
            【前/后段均校验】升级到 SFS 1.2.1 报错，错误码："SFS_OPERATOR_CLUSTER_VERSION_REQUIRED": "选择的版本不合法。
            升级到 SFS 1.3.0 报错，错误码："SFS_OPERATOR_CLUSTER_VERSION_REQUIRED": "选择的版本不合法。
            【前/后段均校验】升级到 SFS 1.2.1-pre1，再升级到SFS 1.2.1 ,可以升级成功（自动化已覆盖）
            部署SFS 1.1.2 文件存储集群，允许直接升级到 sfs 1.2.1
        Tower 4.7.0
            前/后段均校验】部署 SFS 1.1.2文件存储集群, 升级到 1.2.1-pre1 报错，"SFS_OPERATOR_CLUSTER_VERSION_REQUIRED": "选择的版本不合法。
            升级至 SFS 1.2.0成功， 进行除升级外的编辑操作，编辑成功
            【前/后段均校验】升级到 SFS 1.2.1 报错，错误码："SFS_OPERATOR_CLUSTER_VERSION_REQUIRED": "选择的版本不合法。
            升级到 SFS 1.3.0 报错，错误码："SFS_OPERATOR_CLUSTER_VERSION_REQUIRED": "选择的版本不合法。
            【前/后段均校验】升级到 SFS 1.2.1-pre1，再升级到SFS 1.2.1 ,可以升级成功（自动化已覆盖）
            部署SFS 1.1.2 文件存储集群，允许直接升级到 sfs 1.2.1
        AOC 4.7.0
            前/后段均校验】部署 SFS 1.1.2文件存储集群, 升级到 1.2.1-pre1 报错，错误码："SFS_OPERATOR_CLUSTER_VERSION_REQUIRED": "选择的版本不合法。
            升级至 SFS 1.2.0成功， 进行除升级外的编辑操作，编辑成功
            【前/后段均校验】升级到 SFS 1.2.1 报错，错误码："SFS_OPERATOR_CLUSTER_VERSION_REQUIRED": "选择的版本不合法。
            升级到 SFS 1.3.0 报错，错误码："SFS_OPERATOR_CLUSTER_VERSION_REQUIRED": "选择的版本不合法。
            【前/后段均校验】升级到 SFS 1.2.1-pre1，再升级到SFS 1.2.1 ,可以升级成功（自动化已覆盖）
            部署SFS 1.1.2 文件存储集群，允许直接升级到 sfs 1.2.1
    支持界面下载kubeconfig文件
        用户拥有相关权限
            点击进入使用的文件存储集群，「创建」按钮左侧为「下载 Kubeconfig」 图标。
            点击下载按钮，弹出弹窗
            弹窗中展示 Kubeconfig 配置信息，如超出窗口高度，则展示滚动条，支持内部滚动；展示的kubeconfig信息与解析的kubeconfig信息需一致
            点击「复制」按钮可复制 Kubeconfig 配置信息，并弹窗通知：「kubeconfig复制成功」，粘贴内容与复制内容一致，并能够通过复制的kubeconfig文件访问sfs集群
            点击可下载 Kubeconfig 配置信息，下载的文件为 yaml 文件，命名规则为”文件存储集群名称-kubeconfig-日期”。查看下载的文件内容符合预期，并能够通过下载的kubeconfig文件访问sfs集群
            通过断开Tower server pod 网络构造异常
        用户不拥有相关权限
            通过无访问权限（没有sfs管理权限）的用户登陆Tower，点击进入使用的文件存储集群，无法查看到「下载kubeconfig」图标
    SFS-620 (tower 4.6.0 + sfs 1.3.0)
        升级文件存储集群 ：小于1.3.0--->1.3.0，预期成功，kube_vip不存在
        部署 1.3.0 版本的4节点集群，部署成功，kube_vip不存在
        对管理VIP所在的FSC进行下线操作，前/后端均需检查管理VIP迁移至其它未下线节点，并且只有本节点为更新中，其它节点均为正常
        管理 VIP所在的文件控制器的文件管理网卡故障后，前/后端均需检查文件管理VIP发生迁移
        管理 VIP所在的文件控制器的apiserver挂掉，前/后端均需检查文件管理VIP发生迁移
        管理VIP所在的文件控制器的文件存储网卡故障后，前/后端均需检查文件管理VIP发生迁移
        将管理VIP通过修改ManagementVip.status.assigned_node leader 至其它节点，前/后端均需检查文件管理VIP发生迁移
        挂掉2个ETCD后，将管理 VIP所在的文件控制器的文件管理网卡对应的物理网卡故障后，此时管理VIP无法进行迁移，恢复其中1个ETCD，前/后端均需检查文件管理VIP发生迁移
        先升级sfs集群到1.3.0，然后升级tower从4.5.0到4.6.0，预期成功，kube_vip不存在，能够访问managementvip
        生命周期自动化回归测试
    支持动态设置集群的计算资源
        KB 验证：仅验证Tower 4.7.0 + sfs 1.3.0 https://cs.smartx.com/knowledgebase/detail?id=6580&temp=7482
    其他测试
        AOC470 +  AFS 12x UI 界面展示，确保Tower470 相关功能代码不会对老版本ASF造成影响
        Tower 470 + SFS 12x UI 界面展示，确保Tower470 相关功能代码不会对老版本ASF造成影响
Tower 471
    支持为文件系统设置导出路径
        创建文件系统
            【协议支持nfs 访问】创建文件系统时，基本信息-协议选择nfs 访问后，新增导出路径选项如下图（iomesh & acrfra 平台同时测试）
            【导出路径字符校验】导出路径可以是以下 1种或者多种的组合
            【导出路径字符长度校验】导出路径长度小于255时，可以保存成功，且校验后端字段正确
            【导出路径字符长度校验】导出路径长度等于255时，可以保存成功，且校验后端字段正确
            【导出路径字符长度校验】导出路径长度超过255时，不可以保存成功，报错“导出路径支持的字符长度范围为 1-255”
            【导出路径字符长度校验】导出路径长度为0时，不可以保存成功，报错“导出路径支持的字符长度范围为 1-255”
            【导出路径冲突校验】导出路径与已存在的其他已上线的文件系统导出路径同名校验，无法保存成功，提示“导出路径已被文件系统 %filesystem% 使用。” （iomesh & acrfra 平台同时测试）
            【导出路径冲突校验】导出路径与已存在的其他已下线的文件系统导出路径同名，无法保存成功，提示“导出路径已被文件系统 %filesystem% 使用。”
            【导出路径冲突校验】ns1设置导出路径A，将ns1 nfs协议禁用，修改/新建ns2的 导出路径ns1 相同，可以设置成功。且校验后端字段正确。
            【导出路径冲突校验】文件系统A支持nfs协议，且使用默认导出路径，文件系统B的导出路径为 “\ +文件系统A的名字“】无法保存成功，提示”导出路径已被文件系统 %filesystem% 使用。”
            【导出路径冲突校验】导出路径为 “\ +其他不支持nfs协议的文件系统名”，且不与其他文件系统导出路径相同】能够保存成功，且校验后端字段正确
            【协议不支持 nfs 访问】创建文件系统时，基本信息-协议未选择nfs 访问后，无导出路径选项
            检查事件和任务中，有导出路径的描述，且字段正确。
            【导出路径字符合法校验】导出路径字符为上述支持的所有类型的组合，可以保存成功。（UI+自动化）
        编辑文件系统
            编辑导出路径
                【文件系统支持nfs访问】编辑文件系统nfs访问，新增“导出路径”选项（iomesh & acrfra 平台同时测试）
                编辑导出路径页，导出路径相关校验：
                【导出路径字符长度校验】导出路径长度小于255时，可以保存成功，且校验后端字段正确
                【导出路径字符长度校验】导出路径长度等于255时，可以保存成功，且校验后端字段正确
                【导出路径字符长度校验】导出路径长度超过255时，不可以保存成功，报错“导出路径支持的字符长度范围为 1-255”
                【导出路径字符长度校验】导出路径长度为0时，不可以保存成功，报错“导出路径支持的字符长度范围为 1-255”
                【导出路径冲突校验】导出路径与已存在的其他已上线的文件系统导出路径同名校验，无法保存成功，提示“不与当前文件存储集群其他文件系统导出路径相同”+“导出路径已被文件系统 %filesystem% 使用。”
                【导出路径冲突校验】导出路径与已存在的其他已下线的文件系统导出路径同名，无法保存成功，提示“不与当前文件存储集群其他文件系统导出路径相同”+“导出路径已被文件系统 %filesystem% 使用。”
                【导出路径冲突校验】ns1设置导出路径A，将ns1 nfs协议禁用，修改/新建ns2的 导出路径ns1 相同，可以设置成功。且校验后端字段正确。
                【导出路径冲突校验】导出路径为 “\ +其他支持nfs协议的文件系统名”，但不与其他文件系统导出路径相同】无法保存成功，提示“不与当前文件存储集群其他文件系统导出路径相同”+“导出路径已被文件系统 %filesystem% 使用。”
                【导出路径冲突校验】导出路径为 “\ +其他不支持nfs协议的文件系统名”，且不与其他文件系统导出路径相同】能够保存成功，且校验后端字段正确
            编辑 nfs 协议
                NS 支持 nfs 协议，且export path 为默认值，关闭再打开nfs 协议， export path 展示同前值。
                NS 支持 nfs 协议，且export path 为非默认值，关闭再打开nfs 协议， export path 展示同前值。
                NS1 支持 nfs 协议，且 设置了 export path 为A，关闭 nfs 协议， 设置其他NS 的导出路径为A，再打开NS 的nfs 协议，校验报错，提示“不与当前文件存储集群其他文件系统导出路径相同”+“导出路径已被文件系统 %filesystem% 使用。
            上下线文件系统
                NS export path 为默认值，对NS下线 再上线之后， export path 展示同前值。（iomesh & acrfra 平台同时测试）
                NS export path 为非默认值，对NS下线 再上线之后， export path 展示同前值。（iomesh & acrfra 平台同时测试）
                上下线FSC/ SFS集群，对该值无影响，无需测试（已和开发确认）
        升级文件集群
            【tower 461 + sfs 1.2.x】 升级到 【tower 470 + sfs 1.3.0】 （iomesh & acrfra 平台同时测试）
            【tower 461 + sfs 1.2.x】 升级到 【tower 470 + sfs 1.3.0】
        导出路径展示（iomesh & acrfra 平台同时测试）
            在文件系统列表页，展示导出路径，其中：

已上线的NS 展示导出路径符合预期
已下线的NS展示导出路径符合预期
仅支持HDFS协议的导出路径展示为“-”
导出路径超长时，剩余部分展示“……”
导出路径不支持排序
导出路径支持页面筛选
            在支持nfs协议的文件系统详情页，将原来的基本信息进行拆分，其中 nfs 访问下展示导出路径 其内容对应创建文件系统 NFS 设置部分的内容 + 新增的「导出路径」字段。 导出路径」字段位置在「NFS 身份验证」字段前。 挂载路径和导出路径相匹配。
            在不支持nfs协议的文件系统详情页，相关展示同前。
        对mount & umount 影响测试（iomesh & acrfra 平台同时测试）
            修改自定义导出路径之后，通过新的挂载路径，在windows/linux 客户端，可以挂载成功。IO成功后，可以进行umount成功。
            使用导出路径A挂载成功，修改NS导出路径为B，再次使用A挂载，挂载失败，提示“server目标路径不存在”。
            使用导出路径A挂载成功，修改NS导出路径为B，可以对A进行umount成功。
            【windows 平台】验证不同的 windows(3个主测版本)，对创建NS，设置导出路径长度为127，进行多次mount/umout
            1）【windows 平台】创建NS，设置导出路径长度为127 2）windows-1 mount 之后 做IO 3）IO完成之后，修改导出路径长度为1（之前的导出路径在windows上不生效，需手动测试）。 4）windows-2 用新的导出路径 mount 之后 做IO 5）对 步骤4中的 挂载点进行 umount, 期待成功
            1）【linux 平台】创建 NS 1, 设置导出路径为 a, 对其进行mount+IO，写入目录 2）修改 NS 1 导出路径 为 b 3) 创建 NS 2, 设置其导出路径为 a, 用同一个host对其进行mount, mount 成功后，校验目录下为空（确认mount的是一个新的NS2）
            1)【linux 平台】创建NS，设置导出路径长度为1 2）Linux mount 之后 做IO 3）IO同时，修改导出路径（1～255之间随机值），确认进行中的IO不受影响。 4）Linux 用新的导出路径 mount 之后 做IO 5）对 步骤2和步骤4中的 挂载点进行 umount, 期待成功
            自动化创建/编辑导出路径：导出路径空格在最前面 or 最后面的场景
        对 IO的影响测试
            【升级场景】【sfs1.2.x 升级到 sfs 1.3.0】在1.2.x 上，在Linux和Windows 客户端，通过默认导出路径挂载之后，升级过程中以及升级到sfs1.3.0 之后，使用默认挂载点的IO无 中断/error。
            【默认导出路径+ 自定义导出路径 IO】创建文件系统，在Linux和Windows 客户端，使用文件系统默认导出路径（/+文件名）进行挂载，持续进行IO；变更文件系统导出路径为自定义导出路径后，在Linux和Windows 客户端，通过新的挂载路径挂载+IO成功，且之前默认导出路径IO无影响。
            【升级场景】升级之前创建NS，升级之后挂载NS 做IO（确认回归相关用例即可，若没有环境覆盖，需要构造这个场合）
            【故障场景】1、linux -1 用导出路径 A mount 2、修改导出路径 为B，linux -2 再mount 3、ganesha 发生重启/crash 4、验证 linux 1/2 A 和 B mount仍有效
            【修改导出路径+NS下线上线】1）创建NS，设置导出路径长度为1~255的随机字符串，且包含所有有效字符 2）Linux-1 mount之后 做IO, 同时，修改导出路径，验证之前的挂载点不受影响 3）IO 完成，对NS 进行下线 4）NS下线之后，再对NS进行上线 + 修改导出路径 5）Linux-1 用之前的挂载点继续做IO + linux-2 用新的导出路径 mount 之后 做IO 6）对步骤2 和步骤5 的挂载点进行 umoun
    支持展示文件存储集群和文件系统的性能监控图表
        文件存储概览页展示
            历史存储性能指标展示条件
                当文件系统版本 >=1.3 ，且 Tower >= 470 版本时，文件集群概览页面出现历史「存储性能」 widget
                当文件系统版本 < 1.3 ，且 Tower >= 470 版本时，文件集群概览页面仍为 瞬时「性能」 widget
                当文件系统版本 >=1.3 ，且 Tower < 470 版本时(不存在此场景，不测试)
            历史存储性能指标展示详情
                【空状态】该时间范围内没有数据，展示如下，且切换 总/读/写顺畅。
                历史存储性能有数据展示指标如上，详细指标分别为： IOPS I/O平均延时 吞吐量 进行中IO 上述指标又分为：总、读、写（分别可点击） 确认所有图表每30s 自动刷新 仅展示历史时间为过去2小时 不同性能指标在不同单位场景下的UI展示 hover 可同步 展示4 个指标某一时点的数据，并展示 tooltip
            历史性能数据正确性校验
                分别对一下性能数据进行校验
        文件系统监控详情页展示历史存储性能
            【空状态】该时间范围内没有数据，展示如下，提示“该时间段内没有数据”，且切换时间段，仍然展示该提示。
            当文件系统版本 >=1.3 ，且 Tower >= 470 版本时，文件集群概览页面出现历史「存储性能」 widget
            某个文件系统历史存储性能展示指标如上，详细指标分别为： IOPS I/O平均延时 吞吐量 进行中IO
            上述指标又分为：总、读、写（分别可点击）
            默认展示历史时间为过去2小时 可切换展示24H、7D的数据
            hover 可同步 展示4 个指标某一时点的数据，并展示 tooltip：
            NS性能数据正确性校验：需要分别对一下性能数据进行校验（PS: 是否可以选择性校验？） IOPS I/O平均延时 吞吐量 进行中IO
        其他UI展示调整（iomesh 和 arcfra 都覆盖）
            文件服务概览
            文件存储集群概览
            需分别确认 Top5 文件系统、文件控制器、文件系统、集群信息、存储用量的 widget 展示正常。
        异常/特殊场景测试（iomesh & arcfra 都测试）
            集群未关联可观测时
                集群概览页性能展示如下，提示“未关联可观测服务”，且切换 总/读/写顺畅。
                文件系统性能展示为如下，提示“未关联可观测服务”
            集群关联可观测服务连接异常时
                因网络或其他外部错误导致加载失败时（包括可观测性数据获取异常），展示如下，提示“获取数据遇到问题”，且切换 总/读/写顺畅。
                恢复集群与可观测的关联正常之后，集群的性能展示“正常”，文件系统性能图表展示正常。但失联期间的数据展示为空白断点。
                文件系统性能展示为如下，提示“获取数据时遇到问题”。
    NFS 访问的特例规则设计优化
        部署Tower 4.7.0+ SFS 1.3.0
            默认访问权限处点击「读写」按钮
                点击「设置特例规则」，选择只读
                点击「设置特例规则」，选择禁止访问
            默认访问权限处点击「只读」按钮
                点击「设置特例规则」，选择读写
                点击「设置特例规则」，选择禁止访问
            默认访问权限处点击「禁止访问」按钮
                点击「设置特例规则」，选择读写
                点击「设置特例规则」，选择只读
        升级文件存储集群
            升级Tower 4.7.0 + sfs 1.2.1，界面显示与校验与之前一致，编辑之前的内容，仍不支持换行
            升级Tower 4.7.0 + sfs 1.3.0
    支持展示挂载了文件系统的客户端的信息
        文件集群概览页展示
            UI展示「活跃连接」 条件测试
                当文件系统版本 >=1.3 ，且 Tower >= 470 版本时，文件集群概览页面出现 「活跃链接」 tab 以及活跃连接数 widget（iomesh & acrfra 平台同时测试）
                当文件系统版本 < 1.3 ，且 Tower >= 470 版本时，文件集群概览页面不出现 「活跃链接」 tab，无活跃连接数 widget
                当文件系统版本 >=1.3 ，且 Tower < 470 版本时(不存在此场景，不测试)
        文件存储集群活跃连接表格测试
            「活跃连接」表格 UI  测试（iomesh & acrfra 平台同时测试）
                当文件系统版本 >=1.3 ，且 Tower >=470 版本时，文件集群概览页面出现 「活跃链接」 tab，点击活跃连接tab, 跳转至「活跃连接」详情页
                活跃连接为0时，活跃连接详情页展示为：“无活跃连接……”
                活跃连接不为0时，活跃连接详情页展示为具体的活跃连接列表，详情如下：
            活跃连接数数量测试
                【活跃链接数为0】该集群不存在任何client 链接时，活跃链接数展示为0（iomesh & acrfra 平台同时测试）
                【活跃链接数为0】该集群存在client mount 文件系统，但最近48小时内没有任何IO操作，链接数展示为0
                【活跃链接数为1】该集群存在1个 client mount 文件系统后（未进行任何IO），活跃连接数展示为1（mount有IO）
                【活跃链接数为1】该集群存在1个 client mount 文件系统，且该client最近48小时内有IO操作，链接数展示为1
                【活跃链接数为[2,4000]， 多个client 的情况】该集群存在3个 client 通过相同的 协议+vip mount 文件系统，且该3个client最近48小时内有IO操作，链接数展示为3
                【活跃链接数为[2,4000]， 1个client-多个协议 的情况】该集群存在1个 client 分别通过 v3 和 v4.1 mount 文件系统，且该client最近48小时内有IO操作，链接数展示为2
                【活跃链接数为[2,4000]， 1个client-多个 vip 的情况】该集群存在1个 client 分别通过同一个协议， 3个vip mount 同一个 文件系统，且该client最近48小时内有IO操作，链接数展示为3
                【活跃链接数为[2,4000]， 1个client-多个 NS 的情况】该集群存在1个 client mount 3个不同的文件系统，且该client最近48小时内有IO操作，链接数展示为3
                【活跃链接数为[2,4000], 多个client-多种协议-多个vip-多个ns的情况】该集群存在2个 client，分别通过v3和v4.1 ,以及分别通过2个vip  mount 2个不同的文件系统，所有mount近48小时内有IO操作，链接数展示为16（iomesh & acrfra 平台同时测试）
                【活跃链接数为4001】该集群存在大于4000活跃链接数时，活跃链接数展示4000+（测试代价较大），hoerver 提示 “展示最近 48 小时内集群中最新 4000 条客户端连接记录。”
                【活跃连接数更新测试】某个活跃连接访问新的一次访问SFS之后，该活跃连接展示的访问时间会进行更新（不会进行增加）
                【活跃连接数量正确性测试】通过 kubectl get  accessclientmetrics.sfs.iomesh.io  default -oyaml 获 spec -  infos: 下的列表数量，和Tower前端展示 对比，数量要一致。（iomesh & acrfra 平台同时测试）
            特殊场景测试：
                【活跃连接被umount】,对某个活跃连接进行 umount 后，该活跃连接仍存在
                【活跃连接对应的NS被下线】,对某个活跃连接的NS进行下线操作 后，该NS对应的所有活跃连接仍存在。
                【活跃连接对应的NS被删除】,对某个活跃连接的NS进行 删除操作 后，该NS对应的所有活跃连接不存在。
                【活跃连接对应的NS被删除后再重建同名NS】对某个活跃连接的NS1进行删除操作后，该NS1对应的所有活跃连接不存在。重建一个同名的ns1,之前NS1的活跃连接仍不存在。
                【编辑VIP的场景】若vip-a对应活跃连接数，再编辑vip-a→vip-b 后， 活跃vip-a 对应的活跃连接数仍存在。
        文件系统详情展示活跃链接数
            文件系统详情展示「活跃连接」 条件测试
                当文件系统版本 >=1.3 ，且 Tower >= 470 版本时，文件系统详情页出现 「活跃链接」 tab （iomesh & acrfra 平台同时测试）
                当文件系统版本 < 1.3 ，且 Tower >= 470 版本时，文件系统详情页面不出现 「活跃链接」 tab
                当文件系统版本 >=1.3 ，且 Tower < 470 版本时(不存在此场景，不测试)
            文件系统详情展示「活跃连接」 table 测试
                在文件系统详情中，「详情」后增加「活跃连接」tab
                此处「活跃连接」tab 中的数据，仅展示该文件系统的活跃连接（iomesh & acrfra 平台同时测试）
                此处的「活跃连接」可根据客户端IP进行筛选（iomesh & acrfra 平台同时测试）
                【编辑文件系统从NFS协议至HDFS协议】该文件系统的活跃连接在这里仍展示，且在文件集群概览页，该文件系统对应的活跃链接数仍展示
                【活跃连接数量正确性测试通过 kubectl get  accessclientmetrics.sfs.iomesh.io  default -oyaml ｜ grep “ns:$nsId”获取某个 spec -  infos: 下的列表数量，和Tower前端展示 对比，数量要一致。（iomesh & acrfra 平台同时测试）
                【NS被下线】该文件系统的活跃连接在这里仍展示
        kubectl 命令测试
            【iomesh 测试】通过 kubectl get  accessclientmetrics.sfs.iomesh.io  default -oyaml 可以获取集群的活跃连接连接数量信息，且展示仅为48小时内的活跃连接。
            【arcfra】通过 kubectl get  accessclientmetrics.sfs.arcfra.com  default -oyaml 可以获取集群的活跃连接连接数量信息，且展示仅为48小时内的活跃连接。
        异常测试
            【manger leader发生故障】manager leader 所在FSC 下线过程中，不影响连接数量的展示（包括正确性）
            【ganesha 发生重启】不影响连接数量的展示（包括正确性）
            【SFS 集群上下线】对SFS集群下线再重新上线，仍展示48小时内的活跃连接（下线前截图，上线后截图，对比时间点，确认48H内的活跃连接展示正常）
    FSC 内存告警阈值调整
        触发报警和消除报警
        CloudTower 4.7.0 部署 SFS 1.2.1 集群，升级到 CloudTower 4.7.1
        扩容 FSC 的内存资源
        下线 FSC
        下线 SFS 集群
        文件存储集群原本未关联可观测性服务，FSC 虚拟机内存利用率达到 95% 以上后关联可观测性服务
    巡检中心支持 SFS 相关信息采集
        CloudTower 4.1.0 关联 x86_64 CPU 架构的 SMTX OS 6.0.0 集群，部署 1.0.0 版本的 SFS 集群，升级到 CloudTower 4.7.1 和 SMTX OS 6.2.0 后逐版本升级 SFS 到 1.3.0（正常、升级中）
        CloudTower 4.7.1 关联 AARCH64 CPU 架构的 SMTX ZBS 集群，部署 1.3.0 版本的 SFS 集群（部署中、正常、删除中）
        CloudTower 4.7.1 关联 x86_64 CPU 架构的 SMTX ZBS 集群，部署 1.3.0 版本的 SFS 集群时设置错误的管理 IP（部署失败、删除中）
        CloudTower 4.7.1 关联 AARCH64 CPU 架构的 SMTX OS 集群，部署 1.3.0 版本的 SFS 集群，编辑最大带宽，下线/上线 FSC，下线/上线集群，管理 VIP 断连，FSC 关机（更新中、维护中、已下线、内部服务异常、文件控制器异常）
sfs-1.2.2-punan
    功能验证
        UI 基础功能验证
            Tower 462 + sfs-1.2.2 ATOP OS UI 回归测试
            Tower 462 + sfs-1.2.2 ATOP ZBS UI 回归测试
            AOC 462 + afs-1.2.2 ATOP ACOS UI 回归测试
    数据一致性以及高可用验证
        对非预读数据的校验
            对NS2关闭预读，回归历史所有IO用例
            【大文件测试】
            【长稳+压力测试】
            ginkgo  e2e 测试
            chaos e2e 测试
            生命周期+IO测试
            内部进程故障测试
        对预读数据的校验
            正常场景
                用例1： 准备3 台windows + 一个NS，用V3 mount 成功 分别创建3个目录，在每一个目录下： 创建 200 个文件，其中文件的size 分别为(1k,4k,16k,32k,64k,128k,256k,512k,1M,10%,4M,64M分别占5%)进行预读 创建 2000 个文件，其中文件的size 分别为(1k,4k,16k,32k,64k,128k,256k,512k,1M,10%,4M,64M分别占5%)进行预读 创建 10000 个文件，其中文件的size 分别为(1k
                用例2： 准备3 台 linux + 一个NS，用V4.1 mount 成功 分别创建3个目录，在每一个目录下： 创建 200 个文件，其中文件的size 分别为(1k,4k,16k,32k,64k,128k,256k,512k,1M,10%,4M,64M分别占5%)进行预读 创建 2000 个文件，其中文件的size 分别为(1k,4k,16k,32k,64k,128k,256k,512k,1M,10%,4M,64M分别占5%)进行预读 创建 10000 个文件，其中文件的size 分别为(
                用例3： 准备3 台windows + 一个NS，用V3 mount 成功 分别创建3个目录，在每一个目录下： 创建 200 个文件，其中文件的size 分别为(1k,4k,16k,32k,64k,128k,256k,512k,1M,10%,4M,64M分别占5%)进行预读 创建 2000 个文件，其中文件的size 分别为(1k,4k,16k,32k,64k,128k,256k,512k,1M,10%,4M,64M分别占5%)进行预读 创建 10000 个文件，其中文件的size 分别为(1k
                用例4： 准备3 台 linux + 一个NS，用V4.1 mount 成功 分别创建3个目录，在每一个目录下： 创建 100 个文件，其中文件的size 分别为(1k,4k,16k,32k,64k,128k,256k,512k,1M,10%,4M,64M分别占5%)进行预读 创建 1000 个文件，其中文件的size 分别为(1k,4k,16k,32k,64k,128k,256k,512k,1M,10%,4M,64M分别占5%)进行预读 创建 10000 个文件，其中文件的size 分别为(1
                【测试NS打开预读再关闭场景】对NS 打开预读，进行IO，之后，再关闭预读，再进行IO。其中IO包括新的读写+对旧数据的读写。验证整个过程IO无报错。
            故障场景
                【meta 故障】在上述 【用例1】  IO进行中，对1个 meta 注入故障, 确保IO能够成功，且无IO错误。
                【meta 故障】在上述 【用例2】  IO进行中，对1个 meta 注入故障, 确保IO能够成功，且无IO错误。
                【data 故障】在上述 【用例1】  IO进行中，对1个 data 注入故障, 确保IO能够成功，且无IO错误。
                【data 故障】在上述 【用例2】  IO进行中，对1个 data 注入故障, 确保IO能够成功，且无IO错误。
                【 ganesha故障】在上述 【用例1】  IO进行中，对1个 ganesha 注入故障, 确保IO能够成功，且无IO错误。
                【 ganesha 故障】在上述 【用例2】  IO进行中，对1个 ganesha 注入故障, 确保IO能够成功，且无IO错误。
                【 FSC 故障】在上述 【用例1】  IO进行中，对1个 FSC 注入故障(重启), 确保IO能够成功，且无IO错误。
                【 FSC 故障】在上述 【用例2】  IO进行中，对1个 FSC 注入故障（重启）, 确保IO能够成功，且无IO错误。
            运维场景
                【升级场景】在上述 【用例1】  IO进行中，对集群进行升级, 确保IO能够成功，且无IO错误。
                【升级场景】在上述 【用例2】  IO进行中，对1个 meta 注入故障, 确保IO能够成功，且无IO错误。
                【FSC/NS扩容场景不考虑】
                【打开预读之后，对旧数据的读写】部署 1.2.1， 创建的NS，写入数据，升级到1.2.2版之后，打开NS预读，对之前的数据进行读 + 写入新的数据。
                【1.2.2升级场景的IO】1.2.2 升级到1.3.0 过程中包含预读IO，非预读IO(用不同的NS)
            长稳+压力场景
                【windows 长稳新增用例】将上述【用例1】增加执行时长（或者直接写入10000个文件），使用一台Windows，和其他写入大于1M 文件长稳压力用例共用同一个NS（保障既有预读，又有非预读pattern的IO）同时进行，确保IO能够成功，且无IO错误。
                【linux 长稳新增用例】将上述【用例2】增加执行时长，使用一台Linux，和其他写入大于1M 文件长稳压力用例共用同一个NS（保障既有预读，又有非预读pattern的IO）同时进行，确保IO能够成功，且无IO错误。
                【配置文件系统的对象放置策略】在1.2.2 上，打开这个开关，使用长稳，通过一个VIP写入数据，验证这些数据要在该NS不同的shard 上均衡放置
Tower 4.8.0相关
    支持文件集群管理、接入网络在同一网段
        Tower
            Smtx OS
                部署文件存储集群 （Tower 4.7.0 + > sfs 1.3.0-rc.3）
                    文件管理网络与文件接入网络选择的虚拟机网络相同时
                    文件管理网络与文件接入网络选择的虚拟机网络不同时
                升级文件存储集群
                    升级Tower 至 4.7.0，可以升级成功，I/O正常；仍为三张网卡；
                    升级 sfs 1.2.1至 sfs 1.3.0
                        升级前虚拟机网络不同，网段不同  ---->  升级成功 —->更改为同一网段
                        升级前虚拟机网络不同，网段不同  ---->  升级成功---->编辑虚拟机网络相同-----> 更改网段为同一网段
                        升级前虚拟机网络相同，网段不同 —-->升级成功---->更改网段为同一网段
                编辑文件存储集群
                    文件管理网络与文件接入网络选择的虚拟机网络相同时
                    文件管理网络与文件接入网络选择的虚拟机网络不同时
                    编辑成功后，则触发任务“编辑文件存储集群XXX”，事件展示符合预期
            ZBS
                部署文件存储集群
                    若文件集群的 ⸢文件管理虚拟网络分布式交换机⸥ 和 ⸢文件接入虚拟网络分布式交换机⸥ 不同，⸢文件管理虚拟网络 VLAN ID⸥ 和 ⸢文件接入虚拟网络 VLAN ID⸥ 相同
                    若文件集群的 ⸢文件管理虚拟网络分布式交换机⸥ 和 ⸢文件接入虚拟网络分布式交换机⸥ 相同，⸢文件管理虚拟网络 VLAN ID⸥ 和 ⸢文件接入虚拟网络 VLAN ID⸥ 不同
                    若文件集群的 ⸢文件管理虚拟网络分布式交换机⸥ 和 ⸢文件接入虚拟网络分布式交换机⸥ 不同，⸢文件管理虚拟网络 VLAN ID⸥ 和 ⸢文件接入虚拟网络 VLAN ID⸥ 不同
                    若文件集群的 ⸢文件管理虚拟网络分布式交换机⸥ 和 ⸢文件接入虚拟网络分布式交换机⸥ 相同 && ⸢文件管理虚拟网络 VLAN ID⸥ 和 ⸢文件接入虚拟网络 VLAN ID⸥ 相同
                    文件存储集群分布式交换机 and 虚拟网络VLAN ID由不同更新为相同，并将网段更改为相同
                升级文件存储集群
                    升级Tower 至 4.7.0，可以升级成功，I/O正常；仍为三张网卡；
                    升级sfs 1.2.1至sfs 1.3.0
                        升级前分布式交换机 or 虚拟网络VLAN ID不同 ，网段不同 ---->  升级成功---->更改为同一网段
                        升级前分布式交换机 or 虚拟网络VLAN ID不同 ，网段不同 ---->  升级成功---->编辑分布式交换机 and 虚拟网络VLAN ID 相同  ----> 更改为同一网段
                        升级前虚拟机分布式交换机 and 虚拟网络VLAN ID相同 ，网段不同 —--> 升级成功 ----->. 更改网段为同一网段
                编辑文件存储集群
                    若文件集群的 ⸢文件管理虚拟网络分布式交换机⸥ 和 ⸢文件接入虚拟网络分布式交换机⸥ 不同，⸢文件管理虚拟网络 VLAN ID⸥ 和 ⸢文件接入虚拟网络 VLAN ID⸥ 相同
                    若文件集群的 ⸢文件管理虚拟网络分布式交换机⸥ 和 ⸢文件接入虚拟网络分布式交换机⸥ 相同，⸢文件管理虚拟网络 VLAN ID⸥ 和 ⸢文件接入虚拟网络 VLAN ID⸥ 不同
                    若文件集群的 ⸢文件管理虚拟网络分布式交换机⸥ 和 ⸢文件接入虚拟网络分布式交换机⸥ 不同，⸢文件管理虚拟网络 VLAN ID⸥ 和 ⸢文件接入虚拟网络 VLAN ID⸥ 不同
                    若文件集群的 ⸢文件管理虚拟网络分布式交换机⸥ 和 ⸢文件接入虚拟网络分布式交换机⸥ 相同，⸢文件管理虚拟网络 VLAN ID⸥ 和 ⸢文件接入虚拟网络 VLAN ID⸥ 相同
                    编辑成功后，则触发任务“编辑文件存储集群XXX”，事件展示符合预期
        AOC
            部署文件存储集群
                文件管理网络与文件接入网络选择的虚拟机网络相同时
                文件管理网络与文件接入网络选择的虚拟机网络不同时
            升级文件存储集群
                升级Tower 至 4.7.0，可以升级成功，I/O正常；仍为三张网卡；
                升级 sfs 1.2.1至 sfs 1.3.0
                    升级前虚拟机网络不同，网段不同  ---->  升级成功 —->更改为同一网段
                    升级前虚拟机网络不同，网段不同  ---->  升级成功---->编辑虚拟机网络相同-----> 更改网段为同一网段
                    升级前虚拟机网络相同，网段不同 —-->升级成功---->更改网段为同一网段
            编辑文件存储集群
                文件管理网络与文件接入网络选择的虚拟机网络相同时
                文件管理网络与文件接入网络选择的虚拟机网络不同时
                编辑成功后，则触发任务“编辑文件存储集群XXX”，事件展示符合预期
    支持动态设置集群的计算资源
        KB 验证：仅验证Tower 4.7.0 + sfs 1.3.0 https://cs.smartx.com/knowledgebase/detail?id=6580&temp=7482
        Tower
            UI设计
                文件存储集群列表
                文件控制器列表
                增加文件控制器
                编辑计算资源
                编辑最大带宽
            后端验证
                部署文件存储集群
                    查看ganesha不再有memory limit 限制
                    编辑vCPU，例如从8—>16，扩大内存至16
                升级文件存储集群
                    查看ganesha不再有memory limit 限制
                    编辑vCPU以及内存
                    部署文件存储集群sfs 1.2.1 + Tower 4.6.1，升级Tower至 4.7.0 + sfs 1.2.1，不允许通过 UI 编辑 cpu 和 memory
                    部署文件存储集群sfs 1.2.1 + Tower 4.6.1，升级至 Tower 4.7.0 + sfs 1.3.0，允许通过 UI 编辑 cpu 和 memory
        AOC
            AOC Happy Path验证
    1.3.0 不支持 HDFS 和 NFS 的协议互通
        Tower 480 + SFS 130 新建NS场景
    下线文件控制器时关闭「HA」和「随主机开机」
        新部署 CloudTower 4.8.0 和 1.3.0 版本的文件存储集群，下线其中一个文件控制器
        新部署 AOC 4.8.0 和 1.2.1 版本的文件存储集群，下线其中一个文件控制器
        新部署 CloudTower 4.7.1 和 1.2.1 版本的文件存储集群，升级到 CloudTower 4.8.0 和 SFS 1.3.0 后下线文件存储集群
        新部署 AOC 4.6.2 和 1.2.1 版本的文件存储集群，升级到 AOC 4.8.0 和 AFS 1.3.0 后下线文件存储集群
        新部署 CloudTower 4.8.0 和 1.3.0 版本的文件存储集群，扩容文件存储集群后下线新的文件控制器
        新部署 CloudTower 4.8.0 和 1.3.0 版本的文件存储集群，其中一个 FSC 所在的节点断电
        新部署 AOC 4.8.0 和 1.2.1 版本的文件存储集群，下线其中一个文件控制器后将其所在节点进入/退出维护模式，重新上线文件控制器后将文件存储集群升级到 1.3.0，再次下线其中一个文件控制器后将其所在节点进入/退出维护模式
        新部署 CloudTower 4.8.0 和 1.2.1 版本的文件存储集群，下线其中一个文件控制器后将其所在节点进入/退出维护模式，重新上线文件控制器后将文件存储集群升级到 1.3.0，再次下线其中一个文件控制器后将其所在节点进入/退出维护模式
    HDFS IO 性能展示测试
        新部署 SFS 集群并发起 HDFS 读写
        新部署 AFS 集群并发起 HDFS 读写
        新部署 SFS 集群并发起 NFS 读写
        新部署 AFS 集群并发起 NFS 读写
        新部署 SFS 集群并发起 HDFS 和 NFS 读写
        新部署 AFS 集群并发起 HDFS 和 NFS 读写
    HDFS/NFS 不支持协议互通
        新部署 CloudTower 4.8.0 和 1.3.0 版本的文件存储集群
        新部署 AOC 4.8.0 和 1.3.0 版本的文件存储集群
        新部署 CloudTower 4.6.2 和 1.2.1 版本的文件存储集群，升级到 CloudTower 4.8.0 和 SFS 1.3.0
        新部署 AOC 4.7.1 和 1.2.1 版本的文件存储集群，升级到 AOC 4.8.0 和 AFS 1.3.0
    FSC 内上传 island 时带上 tower authorization token 以规避白名单限制
        在 4.8.0 版本的 CloudTower 上部署一个 1.3.0 版本的文件存储集群，然后扩容一个新的 FSC，再将文件存储集群升级到更高的 1.3.0 rc 版本
        在 4.8.0 版本的 AOC 上部署一个 1.3.0 版本的文件存储集群，然后将文件存储集群升级到更高的 1.3.0 rc 版本，再扩容一个新的 FSC
        在 4.7.1 版本的 CloudTower 上部署 1.3.0 版本的文件存储集群，升级到 CloudTower 4.8.0 后将文件存储集群升级到更高的 1.3.0 rc 版本，再扩容一个新的 FSC
        在 4.7.1 版本的 AOC 上部署 1.3.0 版本的文件存储集群，然后扩容一个新的 FSC，再将文件存储集群升级到更高的 1.3.0 rc 版本
        在 4.8.0 版本的 CloudTower 上部署 1.2.0 版本的文件存储集群，然后将文件存储集群依次升级到 1.2.1-pre1，1.2.1 和 1.3.0，再扩容一个新的 FSC
        在 4.7.1 版本的 AOC 上部署 1.2.1 版本的文件存储集群，然后将文件存储集群升级到 1.3.0，再扩容一个新的 FSC
    SFS 适配 Tower480 HA测试
        用例设计和用例执行链接：https://docs.google.com/document/d/1V2PTaxIUdOZEkwbTJCD51xOl5aQRX_aX7z7mucyz8O8/edit?tab=t.0#heading=h.fqi7kzntcfy3
    SFS 适配 8条带测试
        用例设计和执行链接 https://docs.google.com/document/d/19vRTBupTG1Qieze_fU8iLCdTIwp2-ww8qjsMYrhmcik/edit?tab=t.0
SFS 1.3.0
    文件系统支持导出路径
        对 IO的影响测试
            【升级场景】【sfs1.2.x 升级到 sfs 1.3.0】在1.2.x 上，在Linux和Windows 客户端，通过默认导出路径挂载之后，升级过程中以及升级到sfs1.3.0 之后，使用默认挂载点的IO无 中断/error。
            【默认导出路径+ 自定义导出路径 IO】创建文件系统，在Linux和Windows 客户端，使用文件系统默认导出路径（/+文件名）进行挂载，持续进行IO；变更文件系统导出路径为自定义导出路径后，在Linux和Windows 客户端，通过新的挂载路径挂载+IO成功，且之前默认导出路径IO无影响。
            【升级场景】升级之前创建NS，升级之后挂载NS 做IO（确认回归相关用例即可，若没有环境覆盖，需要构造这个场合）
            【故障场景】1、linux -1 用导出路径 A mount 2、修改导出路径 为B，linux -2 再mount 3、ganesha 发生重启/crash 4、验证 linux 1/2 A 和 B mount仍有效
            【修改导出路径+NS下线上线】1）创建NS，设置导出路径长度为1~255的随机字符串，且包含所有有效字符 2）Linux-1 mount之后 做IO, 同时，修改导出路径，验证之前的挂载点不受影响 3）IO 完成，对NS 进行下线 4）NS下线之后，再对NS进行上线 + 修改导出路径 5）Linux-1 用之前的挂载点继续做IO + linux-2 用新的导出路径 mount 之后 做IO 6）对步骤2 和步骤5 的挂载点进行 umoun
        对mount & umount 影响测试（iomesh & acrfra 平台同时测试）
            修改自定义导出路径之后，通过新的挂载路径，在windows/linux 客户端，可以挂载成功。IO成功后，可以进行umount成功。
            使用导出路径A挂载成功，修改NS导出路径为B，再次使用A挂载，挂载失败，提示“server目标路径不存在”。
            使用导出路径A挂载成功，修改NS导出路径为B，可以对A进行umount成功。
            【windows 平台】验证不同的 windows(3个主测版本)，对创建NS，设置导出路径长度为127，进行多次mount/umout
            1）【windows 平台】创建NS，设置导出路径长度为127 2）windows-1 mount 之后 做IO 3）IO完成之后，修改导出路径长度为1（之前的导出路径在windows上不生效，需手动测试）。 4）windows-2 用新的导出路径 mount 之后 做IO 5）对 步骤4中的 挂载点进行 umount, 期待成功
            1）【linux 平台】创建 NS 1, 设置导出路径为 a, 对其进行mount+IO，写入目录 2）修改 NS 1 导出路径 为 b 3) 创建 NS 2, 设置其导出路径为 a, 用同一个host对其进行mount, mount 成功后，校验目录下为空（确认mount的是一个新的NS2）
            1)【linux 平台】创建NS，设置导出路径长度为1 2）Linux mount 之后 做IO 3）IO同时，修改导出路径（1～255之间随机值），确认进行中的IO不受影响。 4）Linux 用新的导出路径 mount 之后 做IO 5）对 步骤2和步骤4中的 挂载点进行 umount, 期待成功
            自动化创建/编辑导出路径：导出路径空格在最前面 or 最后面的场景
    稳定性
        文件控制器系统盘变成 readonly 后自动重启文件控制器
            升级到 1.3.0 后，挂载到目录 /sysroot 的 ext4 文件系统只读
            FSC 下线再重新上线后，挂载到目录 /var/opt/arcfra 的 ext4 文件系统只读
            FSC 上挂载到目录 /var/opt/iomesh/sfs/diag 的 ext4 文件系统只读
            sfs-agent 从故障恢复后，挂载到目录 /var/opt/arcfra/sfs/diag 的 ext4 文件系统只读
HDFS 测试
    HDFS 客户端配置
        客户端版本
            针对不同系统架构(x86、arm) 的 java sdk（名称为：sfs-hadoop-1.0.0.jar），有不同的jar包，均需进行测试
            升级 sfs-hadoop-1.0.0jar 包，I/O不应该中断
        客户端配置项
            fs.AbstractFileSystem.sfs.impl
            fs.sfs.impl
            fs.defaultFS
            Sfs.users && sfs.groups
            sfs.node_name.${HOST_NAME}
            sfs.apiservers
            sfs.kubeconfig.path
        大数据平台对接
            CDH
            HDP
            TBDS
            StarRocks
            Doris
    基本数据IO
        TestDFSIO
            小文件
                写入  200MB * 1000 文件，大小约为 200GB
                把1000 个文件，从200 MB 截断至 100MB
                追加写操作，1000个文件，每个文件追加 200 MB数据
                随机读取 1000 * 200MB 数据
                顺序读取 1000 * 200MB 数据
            大文件
                写入 100 * 4GB 文件 ，查看总的写入量应为400GB左右
                顺序读取 100 × 4GB 数据
                随机读取 100 * 1GB 数据
                逆序读取 100 * 4GB数据
                追加写操作，100个文件，每个文件追加1GB数据
                删除所有文件
        TPC-H
            利用dbgen 模拟生成200GB数据，并将数据加载至Hive中的8张基准表中，随后执行TPC-H 生成的 22 个 Hive QL 测试查询语句
        YCSB
            Load阶段，设置  recordcount为 = 64000000（大约一条数据为1KB，此处相当于写入64GB数据）
            run阶段，设置 operationcount为 1000000，按照下方顺序执行用例
            删除以上数据
            重新Load数据，并运行
    元数据 IO
        创建50000个文件，文件大小分别为4k、64k、128k、1M、8M
        打开50000个文件并读取
        对 HDFS 中 50000个文件执行 rename 操作
        删除 50000个文件
    鉴权
        简单认证
            识别不同linux用户身份
        Kerberos认证
            使用kerberos认证，最终用户解析正确
            支持 IdMapping 实现 Unix 用户到 Uid/Gid 映射
                当自定义和 /etc/passwd相同的用户映射为不同的gid和uid，则应该以自定义中的用户为主
                    配置/etc/passwd，不配置自定义映射
                    仅配置自定义映射，不配置/etc/passwd
                当自定义和LDAP中相同的用户映射为不同的gid和uid，则应该以自定义中的用户为主
                    仅使用SSSD+LDAP系统，不配置自定义映射
    高可用
        sfs集群运维
            Sfs集群下线fsc，不影响io写入的数据一致性，io中断时间符合预期并能够恢复
            Sfs集群上线fsc，不影响io写入的数据一致性，io中断时间符合预期并能够恢复
            扩容sfs集群，不影响io写入的数据一致性，io中断时间符合预期并能够恢复
            【基于feature 升级文件集群时避免重启 FSC进行设计】sfs集群升级过程中，不影响io写入的数据一致性，io中断时间符合预期并能够恢复
        故障场景
            内部进程故障
                对1个apiserver注入docker stop故障，I/O正常
                对3个apiserver注入docker stop故障并恢复
                对1个apiserver注入kill故障，I/O正常
                对1个apiserver注入term故障，I/O正常
            sfs集群故障
                网络故障
                    对1个存储网卡注入故障，I/O正常
                    对3个存储网卡注入故障并恢复
                    对三个接入网卡注入故障
                fsc故障
                    对1个FSC进行reboot操作，I/O正常
            后端故障
                对物理集群的1个主机注入故障，I/O正常
                对1个物理集群的存储网卡注入故障，I/O正常
            后端OS/ZBS集群运维
                X86_64 OS/X86_64 ZBS/ARM64  OS集群升级，不影响sfs集群 io写入的数据一致性，io中断时间符合预期并能够恢复
                X86_64 OS/X86_64 ZBS/ARM64  OS集群故障，不影响sfs集群 io写入的数据一致性，io中断时间符合预期并能够恢复