---
title: "SMTXELF/6.3.0/SMTX ELF 内部服务说明"
source_url: "https://internal-docs.smartx.com/smtxelf/6.3.0/elf_service-list/preface_generic"
sections: 6
---

# SMTXELF/6.3.0/SMTX ELF 内部服务说明
## 关于本文档

# 关于本文档

本文档介绍了 SMTX ELF 的内部服务的功能和部署的节点，以及服务占用的端口和服务失效的影响。

---

## 文档更新信息

# 文档更新信息

**2026-02-04：配合 SMTX ELF 6.3.0 正式发布**

相较于 SMTX ELF 6.2.0，本版本更新如下：

- 新增 `zbx-chunkd` 相关端口。
- 更新 `SSH、SCP` 的默认端口为 22，并支持自定义修改。
- 新增 `consul-exporter` 服务。

---

## 服务功能描述

# 服务功能描述

内部服务是指保证 SMTX ELF（读作 SmartX ELF）的计算和运维功能正常运行所需要的服务。系统既包括 SmartX 独立开发的服务，也包含所依赖的第三方服务。

下表介绍了 SMTX ELF 内部服务的功能和部署的节点。

## 虚拟机相关服务

| **服务名** | **部署节点** | **功能** |
| --- | --- | --- |
| timemachine | 主节点  计算节点 | 提供定时快照功能。 |
| job-center-worker | 主节点  计算节点 | 负责执行异步任务。 |
| job-center-scheduler | 主节点 | 负责发起定时任务。 |
| elf-vm-monitor | 主节点  计算节点 | 该服务是虚拟机 HA 的基础进程，提供 HA 心跳更新、节点文件系统只读检查、业务网络故障检查、触发 HA 等功能。 |
| elf-vm-scheduler | 主节点 | 负责虚拟机自动调度。 |
| elf-dhcp | 主节点 | 提供 DHCP 配置功能。 |
| elf-exporter | 主节点  计算节点 | 采集监控数据。 |
| vnc-proxy | 主节点  计算节点 | 提供 Web VNC 控制台服务。 |
| vmtools-agent | 主节点  计算节点 | 收集虚拟机中运行的 guest agent 汇报的静态信息和性能参数，并将用户需要虚拟机执行的指令发送给虚拟机。 |
| elf-fs | 主节点  计算节点 | 用于存储和管理 OVF 导入过程中用户上传的文件以及 OVF 的导出结果。 |
| ntpm | 主节点  计算节点 | 提供主机时间同步的功能。 |

## 运维相关服务

| **服务名** | **部署节点** | **功能** |
| --- | --- | --- |
| zbs-deploy-server | 主节点  计算节点 | - 提供部署服务。 - 提供运维服务：添加主机、修改管理 IP。 - 提供部署环境配置信息的 API。 |
| cluster-upgrader | 主节点  计算节点 | 提供一键升级集群功能。 |
| tuna-rest-server | 主节点  计算节点 | 提供集群软、硬件管理 RESTful API。 |
| usbredir-manager | 主节点  计算节点 | 管理 usbredirserver 进程，以使本节点的 USB 设备可以被其他节点的虚拟机访问。 |
| vipservice | 主节点  计算节点 | 提供 VIP 服务。 |

## 监控和微服务平台相关服务

| **服务名** | **部署节点** | **功能** |
| --- | --- | --- |
| zbs-watchdogd | 主节点  计算节点 | 提供 HTTP 接口，返回 Chunk 上操作系统是否卡住的结果。 |
| octopus | 主节点  计算节点 | - 从 exporter 中采集数据。 - 将采集的数据通过 API 提供给外部查询。 - 报警检查，并将报警信息发送到 siren。 |
| oscar | 主节点  计算节点 | 管理监控可视化服务的视图和图表。 |
| siren | 主节点  计算节点 | 接收从 octopus 发来的报警条目，并根据邮件和 SNMP 配置将报警进一步通知出去。 |
| aquarium | 主节点  计算节点 | 提供各服务的注册信息。 |
| harbor | 主节点  计算节点 | 该服务把主机请求的 RESTful API 转化为 GRPC 返回给主机。 |
| crab | 主节点  计算节点 | 提供用户认证授权功能。 |
| seal | 主节点 | 提供事件审计服务。 |
| fluent-bit | 主节点  计算节点 | 收集当前节点的审计日志并发给事件中心。 |
| snmpd | 主节点  计算节点 | - 提供 SNMP 数据查询服务。 - 支持通过 SNMP 协议暴露集群监控数据。 |
| network-monitor | 主节点  计算节点 | 提供网络监控服务。 |
| disk-healthd | 主节点  计算节点 | 监控磁盘健康状态。 |
| tuna-exporter | 主节点  计算节点 | 采集监控数据。 |
| svcresctld | 主节点  计算节点 | - 采集节点中 cgroup、服务、进程等不同粒度的监控数据。 - 提供集群服务状态信息接口。 |
| vmagent | 主节点，仅在一个主节点部署。 | 采集监控指标。 |
| vmagent-prod（可观测性） | 主节点  计算节点 | 采集监控指标，并将监控指标发送到 vector。 |
| vector（可观测性） | 主节点  计算节点 | 接收监控指标，采集日志，并将监控指标和日志发送到可观测性服务虚拟机。 |
| consul-exporter | 主节点  计算节点 | 提供 consul-server 的状态指标。 |
| node-exporter | 主节点  计算节点 | 提供主机硬件和操作系统相关指标。 |
| net-health-check | 主节点  计算节点 | 提供网口健康检查功能。 |

## 第三方服务

| **服务名** | **部署节点** | **功能** |
| --- | --- | --- |
| mongod | 主节点  计算节点 | 提供以下服务：  - 存储告警信息。 - 存储虚拟机服务数据库。 - 存储用户数据。 - 存储异步任务。 - 存储注册中心数据。 - 存储多集群的关联信息。 - 存储应用中心的数据。 |
| nginx | 主节点  计算节点 | 提供 API 反向代理和 Web 控制台服务。 |
| chronyd | 主节点  计算节点 | 提供主机时间同步的功能。 |
| zookeeper | 主节点 | 提供分布式一致性协议。 |
| envoy | 主节点  计算节点 | - 提供 nginx 反向代理。 - 提供跨集群的反向代理。 - 集群应用负载均衡。 |
| envoy-xds | 主节点  计算节点 | 提供集群内服务之间的网络功能。 |
| libvirtd | 主节点  计算节点 | 控制虚拟机生命周期。 |
| prometheus | 主节点  计算节点 | 采集监控数据。 |
| consul | 主节点  计算节点 | 提供注册和发现集群内服务的功能。 |
| consul-server | 主节点 | 提供注册和发现集群内服务的功能。 |
| containerd | 主节点  计算节点 | 提供主机容器管理能力。 |
| network-firewall | 主节点  计算节点 | 提供主机防火墙功能。 |

---

## 服务占用端口

# 服务占用端口

为了保证 SMTX ELF 的服务或功能正常使用，用户必须要确保以下 TCP 或 UDP 端口处于可用或者开放状态，并注意：

- 当一个服务使用多个端口时，请确保每个端口随时可被此服务使用。
- 当一个服务只使用某一个端口时，请确保此端口不被其他服务占用。

## 服务占用的固定端口

| 服务名 | 使用端口 |
| --- | --- |
| vipservice | 8777（本地） |
| timemachine | 9912、32768 ~ 52768 |
| zbs-watchdogd | 10300 |
| ntpm | 10414 |
| job-center-worker | 无 |
| job-center-scheduler | 无 |
| zbs-deploy-server | 10403 |
| elf-vm-monitor | 10416 |
| elf-vm-scheduler | 10443 |
| elf-dhcp | 6767 |
| tuna-exporter | 10404 |
| elf-exporter | 10405 |
| log-collector | 10406 |
| vmtools-agent | 6700 |
| vnc-proxy (elf) | 8000（本地）, 5900-5999（本地，虚拟机可能用到的） |
| cluster-upgrader | 8090（仅升级期间 ISO 镜像 repo 使用） |
| turbot | 10409 |
| network-monitor | 10410 |
| disk-healthd | 10415，10480（UDP 端口，仅存储接入网络） |
| octopus | 9900 |
| oscar | 无 |
| siren | 9903 |
| aquarium | 无 |
| harbor | 9980, 20801, 20802, 20803 |
| crab | 9999 |
| envoy | 10000（containerd proxy 端口）、10900、10901、10902 |
| envoy-xds | 24680 |
| consul | 8301 |
| consul-server | 8300、8311(TCP & UDP)、8510 |
| seal | 9923, 9924 |
| snmpd | 1705、161（UDP） |
| fluent-bit | 5170, 20501 |
| mongod | 27017 |
| nginx | 80、443、8500（consul proxy 端口）、9100（node-exporter proxy 端口）、9091（prometheus proxy 端口） |
| chronyd | 123、323（UDP） |
| zookeeper | 2181, 2888, 3888 |
| libvirtd | 16509、16514 |
| prometheus | 9090 |
| SSH、SCP | 22（默认端口，可自定义修改） |
| containerd | 10000 |
| tuna-rest-server | 10411 |
| elf-fs | 28080 |
| svcresctld | 10413 |
| usbredir-manager | 7999（本地）, 7000-7015（虚拟机跨节点访问 USB 设备时使用） |
| vmagent | 8429 |
| vmagent-prod（可观测性） | 8430（仅在关联可观测性服务后使用） |
| vector（可观测性） | 8686（仅在关联可观测性服务后使用） |
| node-exporter | 9100 |
| net-health-check | 12889 |
| network-firewall | 12888 |

## 随机端口说明

在实际场景中，NFS 服务和客户端本地也可能使用一些随机端口，具体说明如下：

- NFS 服务除了使用 `2049` 和 `111` 两个固定端口外，还会启动其他进程（如 status、nlockmgr 等），这些进程的端口由 RPC 服务进行随机分配，每次启动 NFS 服务时使用的端口均不相同。

  若需查看 NFS 当前使用的所有端口，或查看某一未知的端口对应的进程，可采用如下方法：

  - 查看 NFS 当前使用的随机端口：在主机执行 `rpcinfo -p` 命令查看。
  - 查看某一端口对应的服务进程：在主机执行 `netstat -nlp` 命令查看。
- 客户端本地也可能启用一些随机端口对接集群服务。若扫描到此类未知的本地端口，可在主机执行 `netstat -nlp` 命令，查看该端口对应的源端口或目的端口，并根据 SMTX ELF 服务使用的固定端口，确定本地端口对应的集群服务。

---

## 服务失效影响

# 服务失效影响

## 虚拟机相关服务

| **服务名** | **在单节点失效影响** | **整体失效影响** |
| --- | --- | --- |
| timemachine | 只有 Leader 所在节点的 timemachine 可以对外提供服务，该节点 timemachine 失效后集群无法提供定时快照服务，其余节点失效无影响。 | 条件：全部失效   - 定时快照功能失效。 - 跨站点备份/回滚/克隆服务失效。 |
| job-center-worker | - 无法对失效节点上的虚拟机进行管理操作。 - 无法对失效节点进行修改，如修改管理IP、修改该节点 VDS 和对该节点进行插拔盘操作。 - 无法将失效节点的系统资源和服务信息更新到数据库。 - 无法将失效节点的虚拟机运行状态更新到数据库。 | - |
| job-center-scheduler | 无影响 | 条件：全部失效   - 虚拟机管理不可用。 - 监控报警失效。 |
| elf-vm-monitor | 失效节点上的虚拟机无法触发 HA。 | 条件：少于 2 个 有效   - 集群虚拟机 HA 功能失效。 |
| elf-vm-scheduler | - 如果是 Leader 节点上的此服务失效，则虚拟机自动调度功能失效。 - 如果是非 Leader 节点上此服务失效，无影响。 | - |
| elf-dhcp | - 如果是 Leader 节点上的此服务失效，则虚拟机网络DHCP配置功能失效。 - 如果是非 Leader 节点上此服务失效，无影响。 | 无法为虚拟机网络提供 DHCP 配置功能。 |
| elf-exporter | 失效期间，失效节点的监控数据无法获取。 | 集群监控整体失效。 |
| vnc-proxy | 失效节点无法提供 VNC Web 控制台服务。 | - |
| vmtools-agent | - 无法实时汇报该主机上运行的虚拟机的静态信息及性能数据。 - 无法配置该节点上虚拟机的 IP、DNS 等网络信息。 | - 无法通过虚拟机工具实时获取集群所有虚拟机静态数据。 - 无法通过虚拟机工具实时获取集群所有虚拟机性能数据。 - 无法配置集群中所有虚拟机的 IP、DNS 等网络信息。 |
| elf-fs | 失效节点无法响应虚拟机导入导出请求，CloudTower 将向其他正常节点发出请求。不影响失效节点和整个集群的虚拟机导入导出功能。 | 集群的 OVF 导入导出功能无法使用。 |
| ntpm | 失效节点无法执行修改 NTP 配置。 | 条件：全部失效   - 集群无法提供 NTP HA 服务。 - 集群可能出现节点时钟不同步。 |

## 运维相关服务

| **服务名** | **在单节点失效影响** | **整体失效影响** |
| --- | --- | --- |
| zbs-deploy-server | 无法添加主机。 | - |
| cluster-upgrader | 无影响 | - 无法执行一键升级。 - 无法展示历史升级记录。 |
| tuna-rest-server | 失效节点硬件信息 API 无法访问。 | - |
| usbredir-manager | 虚拟机无法访问失效节点的 USB 设备。 | - |
| vipservice | 失效节点无法承载 VIP | 条件：全部失效   - VIP 服务不可用。 |

## 监控和微服务平台服务

| **服务名** | **在单节点失效影响** | **整体失效影响** |
| --- | --- | --- |
| zbs-watchdogd | 若 Zookeeper leader 节点上的 zbs-watchdogd 服务异常，则当节点的操作系统异常时，Zookeeper 服务可能无法快速重新选举。 | - |
| octopus | 该节点上的 Octopus API 不可用，无法通过此节点查询监控数据。 | 条件：全部失效   - 不产生新的监控数据。 - 不产生新的报警。 - 监控数据查询失败。 |
| oscar | 该节点上的 oscar 服务不可用。 | 所有 oscar 服务不可用。 |
| siren | - 在 Leader 上失效：   - 无法接收从 octopus 发来的报警信息。   - 已经实际解决的报警无法显示为解决。   - 无法标记报警已解决。 - 在其他节点失效：该节点上的 siren API 不可用。 | 条件：在 Leader 上失效   - 不发送新的报警通知邮件。 - 报警信息查询失效。 - 无法标记报警已解决。 |
| aquarium | 该节点上的 aquarium 服务不可用，该节点的虚拟机无法访问 aquarium 服务。 | 条件：全部失效   - 无法提供注册信息。 |
| harbor | 该节点上的所有 v3 API 不可用。 | 条件：全部失效   - 所有节点的 v3 API 不可用。 |
| crab | 该节点对应的大部分 API 无法使用。 | 条件：全部失效   - 该集群的大部分 API 无法使用。 |
| seal | 无影响 | 条件：全部失效   - 事件审计不可用 |
| fluent-bit | 节点失效：当前节点的审计日志不会被收集。 | 条件：全部失效   - 集群所有失效节点的审计日志不会被收集。 |
| snmpd | 失效节点无法通过 SNMP 获取数据。 | - 所有节点无法通过 SNMP 获取数据。 |
| network-monitor | 无法获取当前节点网络监控数据。 | 条件：全部失效   - 无法获取网络监控数据。 |
| disk-healthd | - 失效节点的磁盘健康状态无法更新。 - 失效节点无法触发新的磁盘健康状态告警。 - 失效节点已经触发的磁盘健康告警可能与磁盘当前健康状态不符。 | - |
| tuna-exporter | 失效期间，失效节点的监控数据无法获取。 | 集群监控整体失效。 |
| svcresctld | - 失效节点的服务状态异常和资源使用异常的监控告警失效。 - 失效节点的服务状态信息无法获取。 | - 集群整体的服务状态异常和资源使用异常监控告警失效。 - 集群整体的服务状态信息无法获取。 |
| vmagent | 无法采集监控指标数据。 | - |
| vmagent-prod（可观测性） | 该节点无法采集监控指标。 | 条件：全部失效 所有节点无法采集监控指标。 |
| vector（可观测性） | 该节点无法接收监控指标、无法采集日志，且无法将监控指标和日志发送给可观测性服务虚拟机。 | 条件：全部失效 所有节点无法接收监控指标、无法采集日志，且无法将监控指标和日志发送给可观测性服务虚拟机。 |
| consul-exporter | 失效节点无法获取 consul-server 的状态监控数据。 | 条件：全部失效 consul-server 的状态监控整体失效。 |
| node-exporter | 无法提供当前节点的主机硬件和操作系统相关指标。 | 条件：全部失效 无法提供集群中所有节点的主机硬件和操作系统相关指标。 |

## 第三方服务

| **服务名** | **在单节点失效影响** | **整体失效影响** |
| --- | --- | --- |
| mongod | 无影响 | 条件：半数及以上失效   - 所有服务的 API 无法工作。 - Job Center 无法工作。 |
| nginx | 失效节点的 API 和 Web 控制台无法使用。 | - |
| chronyd | - 影响失效节点和其他节点的时钟同步，积累形成时钟差异。 - 时钟差异会导致：    - 部分 IO 延时升高。   - 虚拟机 HA 功能出现误判，导致虚拟机意料外的关机。 | - |
| zookeeper | 无影响 | 条件：在超过半数的节点上服务失效。   - 虚拟机操作大部分不可用。 |
| envoy | 失效节点的 API 无法使用。 | - |
| envoy-xds | 失效节点上的 API 无法正常使用。 | 条件：全部失效   - 所有 API 无法正常使用。 |
| consul | 失效节点上的 API 无法正常使用。 | 条件：全部失效   - 所有 API 无法正常使用。 |
| consul-server | 无影响 | 条件：半数及以上失效   - 所有 API 无法正常使用。 |
| libvirtd | 失效节点上的虚拟机无法进行控制。 | - |
| prometheus | 无影响 | 条件：全部失效   - 不产生新的监控数据。 - 不产生新的报警。 - 监控数据查询失败。 |
| containerd | 失效节点的容器无法被管理。 | - |

---

## 版权信息

# 版权信息

版权所有 © 2026 北京志凌海纳科技股份有限公司（SmartX）保留一切权利。

本文档和本文档包含的全部信息受中华人民共和国法律法规及国际公约对知识产权的管辖和保护。本文档和本文档包含的全部信息的版权由 SmartX 或其许可方所有。未经 SmartX 书面许可，不得以任何方式，包括但不限于电子、机械或光学的方式对本文档内容的部分或全部进行复制、修改、传播、经销、翻印、拆解、存储在检索系统中或其他任何形式的使用。本文档中出现的所有非 SmartX 的公司名称、产品名称和服务名称仅用于识别目的，可能是其各自所有者的注册商标、商标或服务标记，不应被视为未经其所有者书面许可而授予使用前述任何商标、标志的许可或权利，亦不代表该所有者对任何产品或服务信息的授权使用或背书。
